# Mux Video API credentials
# Get these from https://dashboard.mux.com/settings/access-tokens
MUX_TOKEN_ID=
MUX_TOKEN_SECRET=

# Secret for Vercel cron job authentication (optional but recommended)
# Generate with: openssl rand -hex 32
CRON_SECRET=
